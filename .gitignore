# procoder machine state — regenerated, not source
.procoder/state/

# Go build output
/bin/
*.test

# local build artifacts
/dist/
.procoder/index/
.lycheecache
.claude/worktrees/

# binaries a bare `go build ./cmd/...` drops in the repo root
/agent-runtime
/ci-runner
/edge
/engineering-graph
/gates
/gen-openapi
/git-platform
/identity
/mcp-server
/nf
/runner
/work-reviews

# machine-local credentials for the cluster scripts
hack/env.local.sh
